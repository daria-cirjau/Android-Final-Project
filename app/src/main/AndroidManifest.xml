<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.google.androidfinalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_png"
        android:label=" "
        android:roundIcon="@mipmap/ic_launcher_round_png"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidFinalProject">
        <activity
            android:name=".ReservedActivity"
            android:exported="false" />
        <activity
            android:name=".ReserveActivity"
            android:exported="false" />
        <!--
<activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.AndroidFinalProject.NoActionBar" />
        -->
        <activity
            android:name=".LogInActivity"
            android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false"/>
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>-->

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>